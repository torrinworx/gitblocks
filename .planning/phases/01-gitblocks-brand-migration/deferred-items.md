# Deferred Items

- `bl_types/README.md` has an unrelated brand-wording edit outside this plan. It was left untouched and will need a separate docs pass if it should be reconciled.
- `ui/operators.py` still contains legacy `.cozystudio/blocks/` path literals. That surface was out of scope for this storage/redraw migration and should be revisited with the UI/test sweep.
